# Web 文档系统文档

## 页面信息

| 属性 | 值 |
|------|-----|
| 路径 | `/docs` |
| 名称 | Docs |
| 组件 | `src/views/docs/index.vue` |
| 类型 | Vue 3 单页应用 |
| 样式 | VitePress 风格 |

## 页面结构

文档系统采用 VitePress 风格的三栏布局，包含以下主要区域：

1. **左侧边栏** - 文档导航、分类、搜索
2. **中间内容区** - 文档内容展示
3. **右侧目录** - 自动生成的目录导航（TOC）

## 核心功能

### 1. 文档浏览

#### 文档列表

**功能**：
- 显示所有 Markdown 文档
- 显示文档标题、分类、大小、修改时间
- 支持按分类筛选
- 实时搜索文档

**代码位置**：`src/views/docs/index.vue` 第 80-120 行

**关键代码**：
```typescript
const docsList = ref<any[]>([])
const categories = ref<string[]>([])
const currentCategory = ref<string>('全部')

async function loadDocsList() {
  try {
    loading.value = true
    const { data } = await fetchDocsList()
    docsList.value = data.docs || []
    categories.value = ['全部', ...(data.categories || [])]
  } catch (error) {
    console.error('加载文档列表失败:', error)
  } finally {
    loading.value = false
  }
}

const filteredDocs = computed(() => {
  if (searchKeyword.value.trim()) {
    return searchResults.value
  }
  
  if (currentCategory.value === '全部') {
    return docsList.value
  }
  
  return docsList.value.filter(doc => doc.category === currentCategory.value)
})
```

#### 文档阅读

**功能**：
- 点击文档查看内容
- Markdown 渲染
- 代码语法高亮
- 自动生成目录
- 支持深色/浅色主题

**代码位置**：`src/views/docs/index.vue` 第 122-180 行

**关键代码**：
```typescript
const currentDoc = ref<any>(null)
const docContent = ref('')
const tocItems = ref<any[]>([])

async function loadDocContent(doc: any) {
  try {
    currentDoc.value = doc
    const { data } = await fetchDocContent(doc.path)
    docContent.value = data.content || ''
    
    // 生成目录
    await nextTick()
    generateTOC()
  } catch (error) {
    console.error('加载文档内容失败:', error)
  }
}

function generateTOC() {
  const content = docContent.value
  const headings: any[] = []
  const lines = content.split('\n')
  
  lines.forEach((line, index) => {
    const match = line.match(/^(#{1,3})\s+(.+)$/)
    if (match) {
      const level = match[1].length
      const text = match[2]
      const id = text.toLowerCase().replace(/[^\w\s-]/g, '').replace(/\s+/g, '-')
      headings.push({
        text,
        level,
        id,
        index
      })
    }
  })
  
  tocItems.value = headings
}
```

### 2. 搜索功能

#### 文档搜索

**功能**：
- 关键词搜索
- 搜索标题和内容
- 显示匹配的上下文
- 实时搜索结果

**代码位置**：`src/views/docs/index.vue` 第 182-210 行

**关键代码**：
```typescript
const searchKeyword = ref('')
const searchResults = ref<any[]>([])

async function handleSearch() {
  if (!searchKeyword.value.trim()) {
    searchResults.value = []
    return
  }
  
  try {
    loading.value = true
    const { data } = await searchDocs(searchKeyword.value)
    searchResults.value = data.results || []
  } catch (error) {
    console.error('搜索文档失败:', error)
  } finally {
    loading.value = false
  }
}
```

### 3. 主题切换

#### 深色/浅色模式

**功能**：
- 支持深色和浅色主题
- 使用 Naive UI 主题系统
- 实时切换主题

**代码位置**：`src/views/docs/index.vue` 第 212-225 行

**关键代码**：
```typescript
const isDark = ref(true)

function toggleTheme() {
  isDark.value = !isDark.value
}
```

### 4. 响应式布局

#### 自适应设计

**功能**：
- 左侧边栏可折叠
- 右侧目录在小屏幕自动隐藏
- 移动端优化

**代码位置**：`src/views/docs/index.vue` 第 227-250 行

**关键代码**：
```typescript
const collapsed = ref(false)

function handleCollapse() {
  collapsed.value = !collapsed.value
}
```

## API 接口

### 1. 获取文档列表

**端点**：`GET /api/docs/list`

**响应**：
```json
{
  "status": "Success",
  "message": "获取文档列表成功",
  "data": {
    "docs": [
      {
        "id": "blender/00-总览.md",
        "title": "总览",
        "category": "blender",
        "path": "blender/00-总览.md",
        "size": 1024,
        "modified": 1234567890,
        "modified_formatted": "2024-01-01 12:00:00"
      }
    ],
    "categories": ["blender", "web"]
  }
}
```

**代码位置**：`backend/server.py` 第 1950-2020 行

### 2. 获取文档内容

**端点**：`POST /api/docs/content`

**请求**：
```json
{
  "path": "blender/00-总览.md"
}
```

**响应**：
```json
{
  "status": "Success",
  "message": "获取文档内容成功",
  "data": {
    "path": "blender/00-总览.md",
    "content": "# 标题\n内容...",
    "size": 1024
  }
}
```

**代码位置**：`backend/server.py` 第 2022-2070 行

### 3. 获取文档分类

**端点**：`GET /api/docs/categories`

**响应**：
```json
{
  "status": "Success",
  "message": "获取文档分类成功",
  "data": {
    "categories": {
      "blender": {
        "name": "blender",
        "path": "blender",
        "doc_count": 7,
        "subcategories": []
      },
      "web": {
        "name": "web",
        "path": "web",
        "doc_count": 7,
        "subcategories": []
      }
    }
  }
}
```

**代码位置**：`backend/server.py` 第 2072-2120 行

### 4. 搜索文档

**端点**：`POST /api/docs/search`

**请求**：
```json
{
  "keyword": "节点"
}
```

**响应**：
```json
{
  "status": "Success",
  "message": "搜索成功",
  "data": {
    "results": [
      {
        "id": "blender/01-主面板文档.md",
        "title": "主面板文档",
        "category": "blender",
        "path": "blender/01-主面板文档.md",
        "matches": [
          {
            "line": 10,
            "context": "这是一个节点面板的文档\n包含节点相关的说明"
          }
        ]
      }
    ],
    "total": 1
  }
}
```

**代码位置**：`backend/server.py` 第 2122-2177 行

## 组件说明

### 1. 左侧边栏

**组件**：`NLayoutSider`

**功能**：
- 显示文档列表
- 分类筛选
- 搜索功能
- 可折叠

**代码位置**：`src/views/docs/index.vue` 第 280-350 行

### 2. 主内容区

**组件**：`NLayoutContent`

**功能**：
- 显示文档内容
- 欢迎页面
- Markdown 渲染

**代码位置**：`src/views/docs/index.vue` 第 352-450 行

### 3. 右侧目录

**组件**：`NLayoutSider`

**功能**：
- 自动生成目录
- 点击滚动到对应章节
- 支持多级标题

**代码位置**：`src/views/docs/index.vue` 第 452-500 行

## 使用说明

### 1. 访问文档系统

**方法一**：通过导航按钮
- 点击聊天界面头部左侧的文档图标（📖）
- 自动跳转到文档系统

**方法二**：直接访问 URL
- 在浏览器中输入：`http://127.0.0.1:5000/#/docs`

### 2. 浏览文档

1. 在左侧边栏选择分类
2. 点击文档标题查看内容
3. 使用右侧目录快速导航

### 3. 搜索文档

1. 在左侧边栏的搜索框输入关键词
2. 按回车键或点击搜索按钮
3. 在结果列表中点击文档查看

### 4. 切换主题

- 点击顶部导航栏的主题切换按钮
- 在深色和浅色模式之间切换

## 布局结构

### VitePress 风格布局

```
┌────────────────────────────────────────────────────────────┐
│ 左侧边栏 (280px) │ 内容区 (自适应) │ 右侧目录 (240px)    │
│                  │                │                       │
│ [Logo] [主题]    │  [文档标题]    │  [目录]              │
│ [搜索框]         │                │  - 标题1             │
│ [分类菜单]       │  [文档内容]    │  - 标题2             │
│                  │                │    - 子标题2.1       │
│ [文档列表]       │  - 标题1       │  - 标题3             │
│  - 文档1         │  - 内容        │                       │
│  - 文档2         │  - 标题2       │                       │
│                  │  - 内容        │                       │
│                  │  - 标题3       │                       │
│                  │  - 内容        │                       │
│                  │                │                       │
│ [折叠按钮]       │                │                       │
└────────────────────────────────────────────────────────────┘
```

## 样式说明

### Naive UI 组件

| 组件 | 用途 |
|------|------|
| `NLayout` | 布局容器 |
| `NLayoutSider` | 侧边栏 |
| `NLayoutContent` | 内容区 |
| `NMenu` | 菜单 |
| `NCard` | 卡片 |
| `NInput` | 输入框 |
| `NButton` | 按钮 |
| `NTag` | 标签 |
| `NBackTop` | 返回顶部 |

### CSS 变量

使用 Naive UI 的 CSS 变量实现主题切换：

```css
:root {
  --n-color: #ffffff;
  --n-text-color: #333333;
  --n-divider-color: #e5e7eb;
  --n-primary-color: #18a058;
}

.dark {
  --n-color: #1a1a1a;
  --n-text-color: #e5e5e5;
  --n-divider-color: #3c3c3c;
  --n-primary-color: #63e2b7;
}
```

## 响应式设计

| 断点 | 描述 | 行为 |
|------|------|------|
| `lg` | 大屏幕（1024px+） | 显示三栏布局 |
| `md` | 中等屏幕（768px-1023px） | 隐藏右侧目录 |
| `sm` | 小屏幕（<768px） | 隐藏右侧目录，侧边栏可折叠 |

## 性能优化

| 技术 | 用途 |
|------|------|
| 虚拟滚动 | 处理大量文档 |
| 懒加载 | 按需加载文档内容 |
| 防抖 | 优化搜索性能 |
| 缓存 | 缓存文档列表 |

## 可访问性

| 功能 | 实现 |
|------|------|
| 键盘导航 | 支持 Tab 键导航 |
| ARIA 标签 | 使用语义化 HTML |
| 焦点管理 | 自动聚焦搜索框 |
| 屏幕阅读器 | 支持屏幕阅读器 |

## 浏览器兼容性

| 浏览器 | 版本 |
|--------|------|
| Chrome | 90+ |
| Firefox | 88+ |
| Safari | 14+ |
| Edge | 90+ |

## 常见问题

### 1. 文档列表为空

**原因**：
- 后端服务器未启动
- `docs` 文件夹不存在
- 没有 `.md` 文件

**解决方法**：
1. 确认后端服务器已启动
2. 检查 `docs` 文件夹是否存在
3. 确认文件夹中有 `.md` 文件

### 2. 文档内容无法显示

**原因**：
- 文档路径错误
- 文档编码不是 UTF-8
- 文档损坏

**解决方法**：
1. 检查文档路径是否正确
2. 确认文档编码为 UTF-8
3. 检查文档内容是否正确

### 3. 搜索功能无结果

**原因**：
- 关键词不存在
- 搜索功能未正确配置

**解决方法**：
1. 尝试其他关键词
2. 检查后端搜索 API 是否正常

## 更新日志

### v1.0.0 (2024-01-13)

**新增**：
- 文档列表浏览功能
- 文档内容阅读功能
- 关键词搜索功能
- 分类筛选功能
- 深色/浅色主题切换
- 自动生成目录
- 响应式布局

**优化**：
- VitePress 风格界面设计
- 代码语法高亮
- Markdown 渲染优化

**修复**：
- 修复路由跳转问题
- 修复类型错误
- 修复主题切换问题